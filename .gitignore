# API keys — never commit
.env

# Claude Code settings
.claude/

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# Binary outputs (regenerable)
*.pdf

# Step 2 working copy (path workaround — regenerable from root JSON)
src/reference_population.json
